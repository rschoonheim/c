#include <stdio.h>

// Method signature: <return type> <method name>(<parameters>)
int main() {

    printf("Hello, World!\n");

    return 0; // Return 0 to indicate success
              // Return 1 to indicate failure
              // ----------------------------
              // Een programma kan ook gestopt
              // worden met de functie exit().
}